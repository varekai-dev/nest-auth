export const REGX_PASSWORD = /^(?=.*[A-Za-z])(?=.*\d)\S*$/;

export const PASSWORD_FORMAT_IS_NOT_CORRECT =
  'password should not contain space, should contain at least one letter, should contain at least one number';
